# FITS data archive

Runtime FITS archives and manifests for the JWST analysis are stored here.

Large runtime datasets are saved as split compressed archive parts so no single GitHub file exceeds the repository file-size limit. Restore instructions and SHA-256 checksums are generated alongside each archive.
